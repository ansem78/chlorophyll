{
  "name": "chlorophyll",
  "version": "1.0.0",
  "description": "Lightweight CSS framework focused on structure.",
  "scripts": {
    "css": "npm-run-all css-compile css-autoprefix css-rtl css-minify",
    "css-autoprefix": "postcss dist/*.css --use autoprefixer -d dist/",
    "css-compile": "sass --style expanded --no-error-css --stop-on-error scss/:dist/",
    "css-compile-main": "sass --style expanded --no-error-css --stop-on-error scss/chlorophyll.scss dist/chlorophyll.css",
    "css-compile-debug": "sass --style expanded --no-error-css --stop-on-error scss/debug.scss dist/debug.css && sass --style expanded --no-error-css --stop-on-error scss/debug-utils.scss dist/debug-utils.css",
    "css-lint": "stylelint \"**/*.{scss,css}\" --cache --cache-location=.cache/.stylelintcache --rd",
    "css-minify": "npm-run-all --parallel css-minify-*",
    "css-minify-main": "cleancss -O1 --format breakWith=lf --source-map --source-map-inline-sources --output dist/chlorophyll.min.css dist/chlorophyll.css",
    "css-minify-debug": "cleancss -O1 --format breakWith=lf --source-map --source-map-inline-sources --output dist/debug.min.css dist/debug.css && cleancss -O1 --format breakWith=lf --source-map --source-map-inline-sources --output dist/debug-utils.min.css dist/debug-utils.css",
    "css-minify-rtl": "cleancss -O1 --format breakWith=lf --source-map --source-map-inline-sources --output dist/chlorophyll.rtl.min.css dist/chlorophyll.rtl.css",
    "css-rtl": "rtlcss --config .rtlcssrc dist/chlorophyll.css",
    "setup": "yarn install || exit 0",
    "test": "echo \"No test specified.\" && exit 0"
  },
  "author": "Alexandro Morato",
  "keywords": [
    "css",
    "sass",
    "scss",
    "mobile-first",
    "responsive",
    "front-end",
    "framework",
    "web"
  ],
  "style": "dist/chlorophyll.css",
  "sass": "scss/chlorophyll.scss",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ansem78/chlorophyll.git"
  },
  "bugs": {
    "url": "https://github.com/ansem78/chlorophyll/issues"
  },
  "license": "MIT",
  "engines": {
    "node": ">=10.0"
  },
  "files": [
    "dist/*.{css,map}",
    "scss/**/*.scss"
  ],
  "devDependencies": {
    "autoprefixer": "^10.2.3",
    "clean-css-cli": "^4.3.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "rtlcss": "^3.0.0",
    "sass": "^1.32.5",
    "stylelint": "^13.9.0",
    "stylelint-config-chlorophyll": "^1.0.15",
    "stylelint-scss": "^3.18.0"
  }
}
